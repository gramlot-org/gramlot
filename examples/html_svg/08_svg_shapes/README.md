# 08 · SVG shapes

Six SVG primitives in one accessible viewport.

The SVG dialect nests below an HTML figure. Coordinates, fill and stroke are declared as SVG attributes.

**Try it:** Move a shape and compare its coordinates in both languages.

**Limit:** This illustration has no animation or pointer behavior.
