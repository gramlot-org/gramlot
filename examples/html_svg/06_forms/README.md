# 06 · Native forms

Native fields, choices and buttons grouped in fieldsets.

The browser owns editing and Reset. The Source grammar authors the HTML label with `gramlot_label(...)`, because `label` is also a node property.

**Try it:** Change a default value, use Reset and add a select option.

**Limit:** Submit is disabled because this page has no save endpoint or declared form handler.
