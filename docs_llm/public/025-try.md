# Try Gramlot

Document ID: **GC-025**.

> **Release status.** Describes **0.2.0 (HTML/SVG data binding), in development**:
> an approved plan, not yet implemented, tested or released. Latest published
> release: **0.1.2**. Unmarked text is 0.1.2.

This repository contains the bounded native HTML/typed Source development core.
Local packages pass seven Chromium Host profiles; 0.1.0 is owner-accepted for GitHub archive delivery.

<a id="gc-025-005"></a>

## 005 · Choose the current foundation or experimental showcase

For current core behavior read [Writing pages](095-writing-pages.md) and [Classes/hosts](090-classes-and-hosts.md). Python Page authors native Source, while bindings/controllers/resolvers/components remain deferred (*0.2.0:* bindings and controllers in development, not yet tryable; plan in [Writing pages](095-writing-pages.md)). Local examples install package artifacts without sibling-source imports. For richer experimental behavior, download showcase.zip from [Minimal offline showcase](https://github.com/gramlot-org/gramlot-minimal/tree/main/examples/showcase), extract it and open showcase/index.html. Its PoC runtime includes binding, Show source and Inspector; offline RPC lacks its Python backend. Current standalone integration belongs to gramlot-minimal: it runs a JS Page in a Worker through the core shared Host and owns WorkerHost, WorkerTransport, startup and the Node/npm exporter. The old Python/JS compiler is removed.

<a id="gc-025-010"></a>

## 010 · Choose a Host for a page

Python is the first authoring path. Current native HTML hosts are Uvicorn/ASGI, [FastAPI](https://github.com/gramlot-org/gramlot-fastapi/blob/main/docs/050-native-html.md), Flask and Kajenn; JS uses Node, Bun or Worker. Use each adapter's documented revision with locally built artifacts; package registries may differ. [Django](https://github.com/gramlot-org/gramlot-django) and [gramlot-poc](https://github.com/gramlot-org/gramlot-poc) remain experimental paths. PoC behavior does not extend the current core API.

<a id="gc-025-015"></a>

## 015 · What to evaluate

For current core, inspect a Python Page, typed Source, marked remote block, live updates, freeze and cleanup. Evaluate bound fields, actions and Data changes against the experimental showcase's own documentation. Check server failures, required controls, adapter limits and tolerance for API changes. Read [quality](030-quality.md). *0.2.0:* the planned acceptance page (Python plus JS equivalent) combines a title bound to `.settings.caption`, an input, two radios, a checkbox, a computed result, a bound SVG and a button with a nested named `dataController`, with `dataSetter` after some controls; evaluate against it once available.

The core development manual now starts at [Classes and server adapters](090-classes-and-hosts.md). It describes the smaller native HTML foundation separately from the experimental showcase.

The native entry points are FastAPI `NativeHtmlApplication` / `mount_native_html`,
Flask `mount_native_html`, ASGI `NativeHtmlASGI`, Kajenn
`KajennNativeHtmlApplication`, and JS `gramlot-js-server/native` / `gramlot-js-server/bun`.
Use the [Hello World launch profiles](https://github.com/gramlot-org/gramlot-examples/tree/main/apps/hello-world)
with the prepared package artifacts. Legacy `GramlotApplication`, `mount_gramlot`
and PoC launch commands are outside the clean-core contract. Django, the Gramlot site and Rosetta were excluded from the original published
0.1.0 matrix; their PoC capabilities are not implied by that release.


Download the [0.1.0 archive set](https://github.com/gramlot-org/gramlot/releases/tag/v0.1.0).
Extract the complete bundle and follow its README: install all five Python wheels
together in a new environment, or all four npm archives together in a separate
consumer. Dependencies remain floating; internet access is required during setup.
GitHub release assets are the delivery source for this nine-package archive set.
Separately, the JavaScript core was published on JSR as `@genro/gramlot` 0.1.0 on
2026-09-24. That registry publication is not the complete archive set above; its
matching version number does not establish identical contents. Follow the archive
README for this installation path. Later development changes are not implied by
either published 0.1.0 distribution.

Development after 0.1.0 groups these environments into **integration repos**:
`gramlot-fastapi`, `gramlot-flask`, `gramlot-kajenn`, `gramlot-minimal`,
`gramlot-js-server` (Node.js and Bun), and `gramlot-django`. Minimal owns both
Python/ASGI/Uvicorn integration and browser/Worker standalone packaging. Kajenn
consumes minimal's generic ASGI adapter. The renamed development distributions
are `gramlot-minimal`, `gramlot-kajenn` and `@gramlot/minimal`; the published
0.1.0 archives and their original installation instructions remain unchanged.

Minimal and JS Server use `gramlot-minimal` and `gramlot-js-server` locally and
on GitHub. `gramlot-kajenn` is the approved destination name. The inspected local checkout
and its configured origin still use `gramlot-genro-asgi`; a remote rename is not
verified. This naming distinction does not change Kajenn's approved ownership.
Further package publication remains separate work.

Local development now adds native Django integration through `NativeHtmlPages`
and ordinary Django URLconf mounting. Its native profile is separate from the
historical Page/ORM integration; no ORM migration is implied.


<a id="gc-025-020"></a>

## 020 · One shared example suite, several integrations

Gramlot is the upstream reference for framework documentation and the shared
teaching suite: pages, READMEs, runner, logo and theme. Read its manual first,
then the chosen integration's guide; clone that downstream repository for its
adapter, configuration and launch instructions. Read the Docs is the documentation
target, not a claim that all integration sites are already published.

Integrations consume Gramlot's examples without maintaining local source copies.
Python runs in Flask/FastAPI/Django/Kajenn or Minimal with Uvicorn; JavaScript runs
in Node.js/Bun or Minimal's browser Worker. The integration selects the language.
Update the Gramlot dependency, then restart or regenerate the standalone export
to receive changes. Installed environments and exports do not refresh themselves.
Generated assets are outputs, not another source. First-party dependencies remain
unpinned; published 0.1.0 archives are immutable.

Agreed model, not completed ecosystem rollout: sources exist in `examples/html_svg`,
`examples/00-runner` and `themes/gramlot-base`; uniform dependency packaging and
launch commands for all six integrations remain to be implemented and verified.
Historical demos and existing Hello World smoke launchers are separate evidence.

Development naming update: `gramlot-js-server` replaces `gramlot-nodejs` for both Node.js and Bun. Earlier 0.1.0 archive names remain historical.


<a id="gc-025-025"></a>

## 025 · Standalone example download

The ready-to-use example ZIP is distributed through
[gramlot-minimal releases](https://github.com/gramlot-org/gramlot-minimal/releases).
The new thirteen-example runner ZIP is not yet published. The core release's
`gramlot-native-0.1.0.zip` contains package artifacts, not this standalone demo.

Minimal generates the archive from the shared Gramlot examples, runner and theme,
and records the Gramlot version used. Once available, extract the entire archive
and open its `index.html` in a browser; keep the folder together. No application
server or runtime installation is needed. Rebuild or download a new export to
receive updates; an extracted directory does not update itself.
